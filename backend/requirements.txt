Flask>=3.0,<4.0
# YouTube changes what breaks extraction fairly often; keep this current
# (`.venv/bin/pip install -U yt-dlp`) if the audio fallback stops working.
yt-dlp
# Entra ID (Azure AD) sign-in for the public shopping list.
msal>=1.28,<2.0
